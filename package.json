{
  "name": "fetch-ai",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ rabbit-do/fetch-ai",
    "create-local": "graph create --node http://localhost:8020/ rabbit-do/fetch-ai",
    "remove-local": "graph remove --node http://localhost:8020/ rabbit-do/fetch-ai",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 rabbit-do/fetch-ai"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
